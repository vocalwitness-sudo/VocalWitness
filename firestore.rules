rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY WIDE OPEN TEST RULES
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
